[phases.build]
cmds = ["npm run build"]

[start]
# Node directo, sin npm de por medio: así el proceso recibe la señal de cierre
# y se apaga ordenado. Con npm intermedio el apagado se reporta como caída.
cmd = "node peronismogeselino/server/index.js"
