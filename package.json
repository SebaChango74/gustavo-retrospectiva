{
  "scripts": {
    "build": "npm ci --prefix peronismogeselino && npm run build --prefix peronismogeselino",
    "start": "node peronismogeselino/server/index.js"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
